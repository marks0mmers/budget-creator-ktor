ktor {
    development = true
    deployment {
        port = 8000
        watch = [ classes ]
    }
    application {
        modules = [ com.marks0mmers.budgetcreator.ModuleKt.module ]
    }
}

postgresql {
    connectionString = "jdbc:postgresql://localhost:5432/budget-creator"
    connectionString = ${?CONNECTION_STRING}
    connectionString = ${?DATABASE_URL}
    username = "postgres"
    password = "postgres"
    threadPoolSize = 10
}